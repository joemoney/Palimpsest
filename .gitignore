# Environment variables
.env

# Runtime data: per-user saves and the accounts database - not authored content
data/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
venv/
env/
ENV/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Mature/private story content held locally only - NOT committed to this public repo.
# Move to its own private repo and `git submodule add` it when ready (see CLAUDE.md,
# "Public repo, private story content, via git submodules").
stories/the_missing_core/
